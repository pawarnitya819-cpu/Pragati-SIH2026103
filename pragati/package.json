{
  "name": "pragati",
  "private": true,
  "version": "0.1.0",
  "description": "Repo root. The deployable app lives in frontend/ — these scripts just delegate there so a root-level install/build also works.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "npm --prefix frontend ci",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
